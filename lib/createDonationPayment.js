

let createDonationPayment = async () => {
    

};

module.exports = createDonationPayment;

///program add more coin => have to use updateCart => create stripe item for coin
//can you create stripe item that allows client to set total?
//will have to use new CartItem => needs name, code, price, etc.
//unless it's not a cart item and is something else altogether that is added to the stripe
//checkout after
///could be its own cart object, not displayed as an item with the other products;